## 1. Align With Bootstrap

- [x] 1.1 Review the completed `bootstrap-v1-workspace` artifacts and implemented files.
- [x] 1.2 Confirm the finalized workspace naming conventions before creating directories.

## 2. Create Layout

- [x] 2.1 Create active v1 workspace areas for CLI, TypeScript packages, and Python sidecar work.
- [x] 2.2 Add only placeholder, metadata, or documentation files needed to track the layout.
- [x] 2.3 Ensure new workspace files are outside `vendor/used-market-notifier/`.

## 3. Verify Boundaries

- [x] 3.1 Verify no runtime sidecar, engine, storage, notification, or CLI behavior was introduced.
- [x] 3.2 Verify no v2 frontend files or legacy active-root Python app files were created.
- [x] 3.3 Run `openspec status --change prepare-v1-workspace-layout`.
